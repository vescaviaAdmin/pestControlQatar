{
  "name": "qatar-pest-control-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bash ./scripts/build-static.sh",
    "check": "node --check scripts.js && node --check service-data.js && node --check service-detail.js",
    "test": "npm run build && npm run check && node --test tests/static-site.test.mjs"
  }
}
